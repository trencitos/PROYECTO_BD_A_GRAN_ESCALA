version: 2

models:
  - name: fct_sales
    columns:
      - name: transaction_id
        tests:
          - unique
          - not_null
      - name: amount_usd
        tests:
          - dbt_expectations.expect_column_values_to_be_between:
              min_value: 0.01 # Umbral estrictamente positivo