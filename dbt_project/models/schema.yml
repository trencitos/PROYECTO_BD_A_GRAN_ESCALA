version: 2

sources:
  - name: silver
    schema: public
    tables:
      - name: sales_enriched

models:
  - name: fct_sales
    columns:
      - name: transaction_id
        tests:
          - unique
          - not_null
      - name: amount_usd
        tests:
          - dbt_expectations.expect_column_values_to_be_between:
              min_value: 0.01